#include <stdio.h>
#include <string.h>
/*
10. 把一个字符串的内容反转输出
     如:hello world 结果: world hello
*/
char reversalArr(char chs,size_t length){
    for (size_t i = 0; i < length; i++)
    {
        if (chs[i] == " ")
        {
            
        }
        
    }
    
}
int main()
{
    char chs="hello world";
    size_t length = strlen(chs);
    return 0;
}